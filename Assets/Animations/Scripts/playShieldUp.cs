using UnityEngine;
using System.Collections;

public class playShieldUp : MonoBehaviour {

	private bool loop;

	// Use this for initialization
	void Start () {
		loop = false;
	}
	
	// Update is called once per frame
	void Update () {
//		if((Input.GetMouseButtonDown(1)) && (!loop)){
//			animation.Play("camera");
//			loop = true;
//		}
	}
}
